version: '3.7'

services:
  analysis:
    build:
      args:
        - BONSAI_HASH
        - HAROS_HASH
        - PACKAGE
        - PACKAGE_ID
        - PACKAGE_NAME
        - ROS_DISTRO

      context: ./package
      dockerfile: Dockerfile.analysis

    environment:
      - PACKAGE
      - PACKAGE_HASH

    image: "davla/thesis:haros-${HAROS_HASH}-bonsai-${BONSAI_HASH}-${PACKAGE_ID}"

    volumes:
      - type: bind
        source: "${RESULTS_DIR}"
        target: /home/ros/results

  base:
    build:
      args:
        - ROS_DISTRO

      context: .
      dockerfile: Dockerfile.base

    image: davla/thesis:base

  bonsai:
    build:
      args:
        - BONSAI_HASH

      context: ./haros
      dockerfile: Dockerfile.bonsai

    image: "davla/thesis:bonsai-${BONSAI_HASH}"


  haros:
    build:
      args:
        - BONSAI_HASH
        - HAROS_HASH

      context: ./haros
      dockerfile: Dockerfile.haros

    image: "davla/thesis:haros-${HAROS_HASH}-bonsai-${BONSAI_HASH}"

  haros-deps:
    build:
      context: ./haros
      dockerfile: Dockerfile.deps

    image: davla/thesis:haros-deps

  package-build:
    build:
      args:
        - PACKAGE_NAME
        - ROS_DISTRO

      context: ./package
      dockerfile: Dockerfile.build

    image: "davla/thesis:${PACKAGE_ID}"

  dev:
    command: ["bash"]
    environment:
      PACKAGE: dataspeed_ulc_ros/dataspeed_ulc
      PACKAGE_HASH: 'ac692e71'

    image: "davla/thesis:dataspeed_ulc_ros--dataspeed_ulc-ac692e7"

    volumes:
      - type: bind
        source: ../../../bonsai
        target: /src/bonsai-code

      - type: bind
        source: ../..
        target: /home/ros/haros

      - type: bind
        source: ../massive/results
        target: /home/ros/results
